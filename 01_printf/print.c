// stdio.h - standard input output library
#include <stdio.h>
// every C program has to call main function its the starting point for execution
int main()
{
    // c is block structured language and every block can be defined by these braces
    // inside the block we have sequence of statements
    // \n is type of escape sequence used for the newline
    printf("Hello World! \n");
    // compiler automatically assumes that the return value is 0
}